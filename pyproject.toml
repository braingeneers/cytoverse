[project]
name = "cellspace"
version = "0.1.0"
description = "Search and display single cells in foundation model embedding space"
readme = "README.md"
requires-python = ">=3.10, <3.12"
dependencies = []